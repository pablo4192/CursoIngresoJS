function mostrar()
{
	var numeroIngresado;

	for(;;)
	{
		numeroIngresado=prompt("ingrese un numero");
		numeroIngresado=parseInt(numeroIngresado);

		if(numeroIngresado==9)//itera hasta que ingrese el 9, ahi rompe.
		{
			break;
		}	
	}



}//FIN DE LA FUNCIÓN